#include <stdio.h>
 
int main() {
    
    
    float a,b,c,P,A;
    scanf("%f%f%f",&a,&b,&c);
    if((a+b>c)&&(b+c>a)&&(c+a>b)){
        P=a+b+c;
        printf("Perimetro = %0.1f\n",P);
    }
    else{
        A=((a+b)/2)*c;
        printf("Area = %0.1f\n",A);
    }
 
    return 0;
}
